{
  "name": "Bioconductor Devel",
  "image": "bioconductor/bioconductor_docker:devel",
  "platform": "linux/amd64",
  "workspaceFolder": "/work",
  "workspaceMount": "source=${localWorkspaceFolder},target=/work,type=bind",
  "mounts": [
    "source=/run/host-services/ssh-auth.sock,target=/run/host-services/ssh-auth.sock,type=bind"
  ],
  "remoteEnv": {
    "SSH_AUTH_SOCK": "/run/host-services/ssh-auth.sock"
  },
  "containerEnv": {
    "CI": "true",
    "NOT_CRAN": "true"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "mhutchie.git-graph",
        "openai.chatgpt"
      ]
    }
  },
  "postCreateCommand": "git config --global --unset-all core.sshCommand || true && Rscript -e 'pkgs <- c(\"languageserver\", \"httpgd\", \"jsonlite\", \"lintr\", \"diffviewer\"); missing <- pkgs[!vapply(pkgs, requireNamespace, logical(1), quietly = TRUE)]; if (length(missing)) install.packages(missing)'"
}
