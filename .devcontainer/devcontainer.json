{
  "name": "Bioconductor Devel",
  "image": "bioconductor/bioconductor_docker:devel",
  "platform": "linux/amd64",
  "workspaceFolder": "/work",
  "workspaceMount": "source=${localWorkspaceFolder},target=/work,type=bind",
  "mounts": [
    "source=${localEnv:SSH_AUTH_SOCK},target=/ssh-agent,type=bind"
  ],
  "remoteEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  },
  "containerEnv": {
    "CI": "true",
    "NOT_CRAN": "true"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "mhutchie.git-graph",
        "openai.chatgpt"
      ]
    }
  },
  "postCreateCommand": "Rscript -e 'pkgs <- c(\"languageserver\", \"httpgd\", \"jsonlite\", \"lintr\", \"diffviewer\"); missing <- pkgs[!vapply(pkgs, requireNamespace, logical(1), quietly = TRUE)]; if (length(missing)) install.packages(missing)'"
}
